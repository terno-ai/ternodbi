# Admin registrations are intentionally empty.
# When used with TernoAI, the terno app provides detailed admin configurations.
# When used standalone, you can uncomment or add your own admin classes.

from django.contrib import admin

# Models are available for import if needed:
# from .models import (
#     DataSource, Table, TableColumn, ForeignKey,
#     PrivateTableSelector, GroupTableSelector,
#     PrivateColumnSelector, GroupColumnSelector,
#     GroupTableRowFilter, TableRowFilter,
# )

# Uncomment below for standalone usage:
# admin.site.register(DataSource)
# admin.site.register(Table)
# etc.
